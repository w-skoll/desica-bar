name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: macos-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate AppIcon.icns
        run: |
          mkdir -p fish.iconset
          swift make_icon.swift assets/fish.svg fish.iconset 16 32 64 128 256 512 1024
          cp fish.iconset/icon_16.png   fish.iconset/icon_16x16.png
          cp fish.iconset/icon_32.png   fish.iconset/icon_16x16@2x.png
          cp fish.iconset/icon_32.png   fish.iconset/icon_32x32.png
          cp fish.iconset/icon_64.png   fish.iconset/icon_32x32@2x.png
          cp fish.iconset/icon_128.png  fish.iconset/icon_128x128.png
          cp fish.iconset/icon_256.png  fish.iconset/icon_128x128@2x.png
          cp fish.iconset/icon_256.png  fish.iconset/icon_256x256.png
          cp fish.iconset/icon_512.png  fish.iconset/icon_256x256@2x.png
          cp fish.iconset/icon_512.png  fish.iconset/icon_512x512.png
          cp fish.iconset/icon_1024.png fish.iconset/icon_512x512@2x.png
          iconutil -c icns fish.iconset -o assets/AppIcon.icns

      - name: Build app
        run: bash build.sh

      - name: Zip app
        run: zip -r DeSicaBar.zip DeSicaBar.app

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: DeSicaBar.zip
          generate_release_notes: true
